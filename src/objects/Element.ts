export class Element {
  number: number
  symbol: string
  name: string
  // Add other properties as needed

  constructor(number: number, symbol: string, name: string) {
    this.number = number
    this.symbol = symbol
    this.name = name
  }
}
